# Extra dependencies for the sarcasm *classification/detection* phase
# (src/classification/), on top of the base requirements.txt (already
# has pandas, scikit-learn, transformers, torch, etc. -- reused as-is).
# Install alongside the base requirements:
#   pip install -r requirements.txt -r requirements-classification.txt

# DSPy-optimized LLM classification (M5)
dspy>=2.5.0

# Required by transformers.Trainer for the fine-tuning phase (M6) in
# current transformers versions (this repo pins transformers<5.0.0).
accelerate>=0.26.0

# Required by DebertaV2Tokenizer (M6, microsoft/deberta-v3-base) --
# use_fast_tokenizer=false is used throughout this project for M6 (see
# EXPERIMENT_LOG.md), which loads the slow, SentencePiece-backed
# tokenizer rather than the fast Rust one.
sentencepiece>=0.2.0
